spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
      hibernate:
        ddl-auto: create
  sql:
    init:
      mode: always

logging:
  level:
    root: WARN
    com.bantads.cliente: INFO
